// Client SDK entry. Skeleton — implementation lands in Phase 4.
import { SHARED_TYPES_VERSION } from '@license-engine/shared-types';

export const SDK_VERSION = '0.1.0' as const;
export { SHARED_TYPES_VERSION };
